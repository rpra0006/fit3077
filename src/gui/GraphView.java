package gui;

import model.PatientMonitor;

public abstract class GraphView extends MonitorView {
	/*
	 * Abstract class of GraphView objects
	 */
	
	public GraphView(PatientMonitor monitor) {
		super(monitor);
	}

}
